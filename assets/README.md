# Game assets

Keep source and export formats together where licensing allows:

- `audio/`: ambience, danger stingers and relic/victory cues;
- `cards/`: print masters and card-recognition markers;
- `textures/`: UI and board textures;
- `models/`: render assets, distinct from printable CAD under `hardware/`.

Every contributed asset should include author, license and source metadata.
